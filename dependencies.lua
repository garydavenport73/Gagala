require('weapon')
require('ship')
require('customObject') -- a generic object that is added to a list (table) which can be iterated over
require('customObjectsList') -- a dynamic table that contains references to the customObjects, can be looped over
require('alienRaid') --demonstrates using the customObject and customObject list to make an destroy objects and loop over them
require('collectibleDefs')
require('alienDefs')
require('loadAliens')
require('gagalaRaid')